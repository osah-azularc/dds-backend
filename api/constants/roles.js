// Role names as stored in `roles.name` / `judge_assistant_clerk.user_type`.
// Only these roles are permitted to log in to DDS.
export const DDS_ROLES = ["dds_clerk", "dds_helpdesk", "dds_superuser"];
